# 项目目录结构文档

## 1. 项目整体结构

```
shuyuanquchong/
├── frontend/                    # 前端项目
│   ├── public/                  # 静态资源
│   ├── src/                     # 源代码
│   ├── index.html               # HTML 模板
│   ├── vite.config.js           # Vite 配置
│   └── package.json             # 项目配置
│
├── backend/                     # 后端项目
│   ├── app/                     # 应用代码
│   ├── tests/                   # 测试代码
│   ├── requirements.txt         # Python 依赖
│   └── requirements-dev.txt     # 开发依赖
│
├── docs/                        # 项目文档
│   ├── UI设计规范.md
│   ├── 系统架构设计.md
│   ├── API接口设计.md
│   ├── 前端组件设计.md
│   ├── 项目目录结构.md
│   ├── 网站功能规划.md
│   └── sessions/                # 会话总结文档
│       └── SESSION_01_URL_DEDUPLICATION_OPTIMIZATION.md
│
├── CLAUDE.md                    # AI 开发规范文件
├── start.bat                    # 一键启动脚本 (Windows)
├── stop.bat                     # 停止服务脚本 (Windows)
├── .gitignore                   # Git 忽略文件
└── README.md                    # 项目说明
```

---

## 2. 前端目录结构

```
frontend/
├── public/                          # 静态资源（不经过构建）
│   └── favicon.ico                  # 网站图标
│
├── src/                             # 源代码目录
│   ├── api/                         # API 接口封装
│   │   └── sources.js               # 书源相关 API
│   │
│   ├── components/                  # Vue 组件
│   │   ├── FileUpload.vue           # 文件上传组件
│   │   ├── UrlInput.vue             # URL 输入组件
│   │   ├── ActionButtons.vue        # 操作按钮组
│   │   ├── StatsCard.vue            # 统计卡片组件
│   │   ├── StatsCardGroup.vue       # 统计卡片组
│   │   ├── DuplicateUrls.vue        # 重复URL展示组件
│   │   ├── DownloadButton.vue       # 下载按钮组件
│   │   └── MessageAlert.vue         # 消息提示组件
│   │
│   ├── composables/                 # 组合式函数（Composables）
│   │   └── useSources.js            # 书源状态管理
│   │
│   ├── utils/                       # 工具函数
│   │   └── download.js              # 文件下载
│   │
│   ├── styles/                      # 样式文件
│   │   └── index.css                # 全局样式
│   │
│   ├── App.vue                      # 根组件
│   └── main.js                      # 应用入口
│
├── index.html                       # HTML 模板
│
├── vite.config.js                   # Vite 配置
│
└── package.json                     # 项目配置
```

### 2.1 关键文件说明

#### package.json
```json
{
  "name": "shuyuanquchong-frontend",
  "version": "1.0.0",
  "description": "阅读书源去重校验工具",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.4.0",
    "element-plus": "^2.5.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "vite": "^5.0.0",
    "sass": "^1.69.0"
  }
}
```

#### vite.config.js
```javascript
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': resolve(__dirname, 'src')
    }
  },
  server: {
    port: 5173,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true
      }
    }
  },
  build: {
    outDir: 'dist',
    sourcemap: false
  }
})
```

---

## 3. 后端目录结构

```
backend/
├── app/                             # 应用代码目录
│   ├── __init__.py                  # 包初始化
│   │
│   ├── main.py                      # FastAPI 应用入口
│   │
│   ├── config.py                    # 配置管理
│   │
│   ├── api/                         # API 路由
│   │   ├── __init__.py
│   │   ├── health.py                # 健康检查路由
│   │   └── sources.py               # 书源解析路由
│   │
│   ├── services/                    # 业务逻辑层
│   │   ├── __init__.py
│   │   ├── parser.py                # JSON 解析服务
│   │   ├── deduper.py               # 去重服务
│   │   └── validator.py             # 校验服务
│   │
│   ├── models/                      # 数据模型
│   │   ├── __init__.py
│   │   ├── request.py               # 请求模型
│   │   └── response.py              # 响应模型
│   │
│   └── utils/                       # 工具函数
│       └── __init__.py
│
├── tests/                           # 测试目录
│   ├── __init__.py
│   ├── conftest.py                  # pytest 配置
│   ├── test_parser.py               # 解析器测试
│   ├── test_deduper.py              # 去重测试
│   └── test_validator.py            # 校验测试
│
├── requirements.txt                 # Python 依赖
│
└── requirements-dev.txt             # 开发依赖
```

### 3.1 关键文件说明

#### requirements.txt
```
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
httpx>=0.26.0
```

#### requirements-dev.txt
```
-r requirements.txt
pytest>=7.4.0
pytest-asyncio>=0.23.0
```

---

## 4. 文档目录结构

```
docs/
├── UI设计规范.md
├── 系统架构设计.md
├── API接口设计.md
├── 前端组件设计.md
├── 项目目录结构.md
├── 网站功能规划.md
└── sessions/                        # 会话总结文档目录
    ├── SESSION_01_URL_DEDUPLICATION_OPTIMIZATION.md
    └── SESSION_02_CONCURRENT_VALIDATION.md
```

---

## 5. 启动脚本

### 5.1 start.bat (Windows)
```batch
@echo off
echo ========================================
echo   Book Source Tool - Starting...
echo ========================================
echo.

:: Check Python
python --version >nul 2>&1
if errorlevel 1 (
    echo [ERROR] Python not found. Please install Python 3.10+
    pause
    exit /b 1
)

:: Check Node.js
node --version >nul 2>&1
if errorlevel 1 (
    echo [ERROR] Node.js not found. Please install Node.js 18+
    pause
    exit /b 1
)

echo [1/4] Installing backend dependencies...
cd backend
pip install -r requirements.txt -q

echo [2/4] Installing frontend dependencies...
cd ..\frontend
call npm install --silent

echo [3/4] Starting backend server (port 8000)...
cd ..\backend
start "Backend" cmd /k python -m uvicorn app.main:app --reload --port 8000

echo [4/4] Starting frontend server (port 5173)...
cd ..\frontend
start "Frontend" cmd /k npm run dev

cd ..

echo.
echo ========================================
echo   Started successfully!
echo ========================================
echo.
echo   Frontend: http://localhost:5173
echo   API Docs: http://localhost:8000/docs
echo.
echo   Press any key to open browser...
pause >nul

start http://localhost:5173
```

### 5.2 stop.bat (Windows)
```batch
@echo off
echo ========================================
echo   Stopping all services...
echo ========================================
echo.

:: Stop backend
taskkill /f /im python.exe 2>nul
echo [OK] Backend stopped

:: Stop frontend
taskkill /f /im node.exe 2>nul
echo [OK] Frontend stopped

echo.
echo All services stopped.
pause
```

---

## 6. Git 忽略配置

```gitignore
# .gitignore

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
ENV/
.venv/
*.egg-info/
dist/
build/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build
frontend/dist/
backend/dist/

# IDE
.idea/
.vscode/
*.swp
*.swo
.DS_Store

# Environment
.env
.env.local
.env.*.local

# Logs
logs/
*.log

# Testing
.coverage
htmlcov/
.pytest_cache/

# Temporary
*.tmp
*.temp
```

---

## 7. 项目初始化步骤

### 7.1 方式一：一键启动（推荐）
```
双击运行 start.bat
```

### 7.2 方式二：手动启动

**初始化后端：**
```bash
cd backend
pip install -r requirements.txt
```

**初始化前端：**
```bash
cd frontend
npm install
```

**启动服务：**
```bash
# 后端
cd backend
uvicorn app.main:app --reload --port 8000

# 前端
cd frontend
npm run dev
```

### 7.3 访问应用
- 前端: http://localhost:5173
- API: http://localhost:8000
- API 文档: http://localhost:8000/docs

---

## 8. 文件命名规范

### 8.1 前端命名
- 组件: PascalCase (如 `FileUpload.vue`)
- 工具函数: camelCase (如 `download.js`)
- 样式: kebab-case 或 camelCase

### 8.2 后端命名
- 模块: snake_case (如 `parser.py`)
- 类: PascalCase (如 `ParserService`)
- 函数/方法: snake_case (如 `parse_source`)
- 常量: UPPER_SNAKE_CASE (如 `MAX_FILE_SIZE`)

---

文档版本：v1.3
最后更新：2026-03-12