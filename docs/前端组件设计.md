# 前端组件设计文档

## 1. 组件概述

### 1.1 组件架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                           App.vue                               │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                      Header                                │  │
│  │            📚 阅读书源 · 净源工坊                          │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                   MessageAlert                             │  │
│  │              (成功/错误/警告提示)                           │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────┐  ┌────────────────────────────┐    │
│  │     FileUpload         │  │       UrlInput             │    │
│  │    (文件上传组件)       │  │    (URL输入组件)           │    │
│  └────────────────────────┘  └────────────────────────────┘    │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                   ActionButtons                            │  │
│  │     [ 🔍 只查重复 ]    [ ✅ 全部校验 ]                     │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    ProgressBar                             │  │
│  │   [████████████░░░░░░░░] 50/100 正在校验...  [取消]       │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                   StatsCardGroup                           │  │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐         │  │
│  │  │StatsCard│ │StatsCard│ │StatsCard│ │StatsCard│         │  │
│  │  │ 总数    │ │ 去重后  │ │格式失效 │ │深度失效 │         │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘         │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                    DownloadButton                          │  │
│  │              ⬇️ 下载去重后的JSON                           │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │                      Footer                                │  │
│  │       ✦ 去重基于 bookSourceUrl 字段...                     │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 组件列表

| 组件名 | 路径 | 描述 |
|--------|------|------|
| App | /src/App.vue | 根组件，管理全局状态 |
| FileUpload | /src/components/FileUpload.vue | 文件上传组件 |
| UrlInput | /src/components/UrlInput.vue | URL 输入组件 |
| ActionButtons | /src/components/ActionButtons.vue | 操作按钮组（只查重复/全部校验） |
| ProgressBar | /src/components/ProgressBar.vue | 校验进度条组件 |
| StatsCard | /src/components/StatsCard.vue | 统计卡片组件 |
| StatsCardGroup | /src/components/StatsCardGroup.vue | 统计卡片组 |
| DownloadButton | /src/components/DownloadButton.vue | 下载按钮组件 |
| MessageAlert | /src/components/MessageAlert.vue | 消息提示组件 |
| LoadingSpinner | /src/components/LoadingSpinner.vue | 加载动画组件 |

---

## 2. 状态管理

### 2.1 全局状态 (App.vue)

```javascript
// composables/useSources.js
import { reactive, computed } from 'vue'

/**
 * 使用书源状态管理
 */
export function useSources() {
  const state = reactive({
    // 文件状态
    file: null,
    fileName: null,

    // 加载状态
    loading: false,
    validating: false,

    // 消息状态
    error: null,
    success: null,
    warning: null,

    // 数据状态
    sourceData: null
  })

  // 计算属性
  const hasFile = computed(() => !!state.file)

  const hasValidSources = computed(() =>
    state.sourceData && state.sourceData.validCount > 0
  )

  const stats = computed(() => {
    if (!state.sourceData) return null
    return {
      total: state.sourceData.total,
      dedupCount: state.sourceData.dedupCount,
      formatInvalid: state.sourceData.formatInvalid,
      deepInvalid: state.sourceData.deepInvalid,
      validCount: state.sourceData.validCount
    }
  })

  // 方法
  function setFile(file) {
    state.file = file
    state.fileName = file.name
    state.sourceData = null
    state.error = null
    state.success = null
    state.warning = null
  }

  function setLoading(value) {
    state.loading = value
  }

  function setValidating(value) {
    state.validating = value
  }

  function setError(message) {
    state.error = message
    state.success = null
    state.warning = null
  }

  function setSuccess(message) {
    state.success = message
    state.error = null
    state.warning = null
  }

  function setWarning(message) {
    state.warning = message
    state.error = null
    state.success = null
  }

  function setSourceData(data) {
    state.sourceData = data
    if (data.formatInvalid > 0 || (data.deepInvalid && data.deepInvalid > 0)) {
      const invalidTotal = data.formatInvalid + (data.deepInvalid || 0)
      setWarning(`发现 ${invalidTotal} 条失效书源`)
    }
  }

  function reset() {
    state.file = null
    state.fileName = null
    state.loading = false
    state.validating = false
    state.error = null
    state.success = null
    state.warning = null
    state.sourceData = null
  }

  function clearMessages() {
    state.error = null
    state.success = null
    state.warning = null
  }

  return {
    state,
    hasFile,
    hasValidSources,
    stats,
    setFile,
    setLoading,
    setValidating,
    setError,
    setSuccess,
    setWarning,
    setSourceData,
    reset,
    clearMessages
  }
}
```

---

## 3. 组件详细设计

### 3.1 App.vue - 根组件

#### 职责
- 管理全局状态
- 协调子组件通信
- 处理 API 调用

#### 模板结构
```vue
<template>
  <div class="app-container">
    <!-- 头部 -->
    <header class="app-header">
      <h1 class="title">📚 阅读书源 · 净源工坊</h1>
      <p class="subtitle">⚡ 上传/在线解析 · 自动去重深度校验</p>
    </header>

    <!-- 消息提示 -->
    <MessageAlert
      v-if="state.error || state.success || state.warning"
      :error="state.error"
      :success="state.success"
      :warning="state.warning"
      @close="clearMessages"
    />

    <!-- 操作区域 -->
    <main class="main-content">
      <div class="input-section">
        <FileUpload
          :loading="state.loading || state.validating"
          :file-name="state.fileName"
          @upload="handleFileUpload"
        />

        <UrlInput
          :loading="state.loading || state.validating"
          @fetch="handleUrlFetch"
        />
      </div>

      <!-- 操作按钮 -->
      <ActionButtons
        v-if="hasFile"
        :loading="state.loading"
        :validating="state.validating"
        @dedup="handleDedupOnly"
        @full-validate="handleFullValidate"
      />

      <!-- 统计结果 -->
      <section class="stats-section" v-if="state.sourceData">
        <h2 class="section-title">📊 统计结果</h2>
        <StatsCardGroup :stats="stats" />

        <DownloadButton
          :enabled="hasValidSources"
          :count="state.sourceData?.validCount || 0"
          @download="handleDownload"
        />
      </section>

      <!-- 空状态提示 -->
      <section class="empty-section" v-else-if="!hasFile">
        <p class="empty-text">上传文件或输入链接开始处理书源</p>
      </section>
    </main>

    <!-- 页脚 -->
    <footer class="app-footer">
      <p class="footer-text">
        ✦ 去重基于 bookSourceUrl 字段，深度校验模拟手机端请求检测可用性
      </p>
    </footer>
  </div>
</template>

<script setup>
import FileUpload from './components/FileUpload.vue'
import UrlInput from './components/UrlInput.vue'
import ActionButtons from './components/ActionButtons.vue'
import StatsCardGroup from './components/StatsCardGroup.vue'
import DownloadButton from './components/DownloadButton.vue'
import MessageAlert from './components/MessageAlert.vue'
import { useSources } from './composables/useSources.js'
import { parseFile, parseUrl } from './api/sources.js'
import { downloadJson, formatDate } from './utils/download.js'

const {
  state,
  hasFile,
  hasValidSources,
  stats,
  setFile,
  setLoading,
  setValidating,
  setError,
  setSuccess,
  setWarning,
  setSourceData,
  clearMessages
} = useSources()

// 处理文件上传
function handleFileUpload(file) {
  setFile(file)
}

// 处理 URL 获取
async function handleUrlFetch(url) {
  setLoading(true)
  setError(null)

  try {
    const result = await parseUrl(url, 'dedup')

    if (result.code !== 200) {
      setError(result.message)
      return
    }

    setSourceData(result.data)
    setSuccess(`获取成功！共 ${result.data.total} 条书源`)
  } catch (err) {
    setError('网络错误，请稍后重试')
  } finally {
    setLoading(false)
  }
}

// 只查重复
async function handleDedupOnly() {
  if (!state.file) return

  setLoading(true)
  setError(null)

  try {
    const result = await parseFile(state.file, 'dedup')

    if (result.code !== 200) {
      setError(result.message)
      return
    }

    setSourceData(result.data)
    setSuccess(`去重完成！有效书源 ${result.data.validCount} 条`)
  } catch (err) {
    setError('处理失败，请稍后重试')
  } finally {
    setLoading(false)
  }
}

// 全部校验
async function handleFullValidate() {
  if (!state.file) return

  setValidating(true)
  setError(null)

  try {
    const result = await parseFile(state.file, 'full')

    if (result.code !== 200) {
      setError(result.message)
      return
    }

    setSourceData(result.data)
    setSuccess(`校验完成！有效书源 ${result.data.validCount} 条`)
  } catch (err) {
    setError('校验失败，请稍后重试')
  } finally {
    setValidating(false)
  }
}

// 处理下载
function handleDownload() {
  if (!state.sourceData?.dedupedSources) return

  downloadJson(
    state.sourceData.dedupedSources,
    `阅读书源_去重有效_${formatDate(new Date())}.json`
  )
}
</script>
```

---

### 3.2 ActionButtons.vue - 操作按钮组件

```vue
<template>
  <div class="action-buttons">
    <el-button
      type="primary"
      size="large"
      :loading="loading"
      :disabled="validating"
      @click="$emit('dedup')"
    >
      <span class="btn-icon">🔍</span>
      <span>只查重复</span>
      <span class="btn-hint">快速去重</span>
    </el-button>

    <el-button
      type="success"
      size="large"
      :loading="validating"
      :disabled="loading"
      @click="$emit('full-validate')"
    >
      <span class="btn-icon">✅</span>
      <span>全部校验</span>
      <span class="btn-hint">去重+深度校验</span>
    </el-button>
  </div>
</template>

<script setup>
defineProps({
  loading: Boolean,
  validating: Boolean
})

defineEmits(['dedup', 'full-validate'])
</script>
```

---

### 3.3 StatsCard.vue - 统计卡片组件

```vue
<template>
  <div class="stats-card" :class="`theme-${theme}`">
    <div class="stats-value">{{ displayValue }}</div>
    <div class="stats-label">{{ label }}</div>
  </div>
</template>

<script setup>
import { computed } from 'vue'

const props = defineProps({
  value: [Number, String],
  label: String,
  theme: {
    type: String,
    default: 'primary'
  }
})

const displayValue = computed(() => {
  if (typeof props.value === 'number') {
    return props.value.toLocaleString()
  }
  return props.value
})
</script>
```

---

### 3.4 StatsCardGroup.vue - 统计卡片组

```vue
<template>
  <div class="stats-card-group">
    <StatsCard
      :value="stats.total"
      label="总书源数"
      theme="primary"
    />
    <StatsCard
      :value="stats.dedupCount"
      label="去重后"
      theme="primary"
    />
    <StatsCard
      :value="stats.formatInvalid"
      label="格式失效"
      theme="warning"
    />
    <StatsCard
      v-if="stats.deepInvalid !== null"
      :value="stats.deepInvalid"
      label="深度失效"
      theme="warning"
    />
  </div>
</template>

<script setup>
import StatsCard from './StatsCard.vue'

defineProps({
  stats: {
    type: Object,
    required: true
  }
})
</script>
```

---

## 4. 工具函数

### 4.1 文件下载工具

```javascript
// utils/download.js

/**
 * 下载JSON文件
 * @param {any} data - JSON数据
 * @param {string} filename - 文件名
 */
export function downloadJson(data, filename) {
  const json = JSON.stringify(data, null, 2)
  const blob = new Blob([json], { type: 'application/json' })
  const url = URL.createObjectURL(blob)

  const link = document.createElement('a')
  link.href = url
  link.download = filename
  document.body.appendChild(link)
  link.click()
  document.body.removeChild(link)

  URL.revokeObjectURL(url)
}

/**
 * 格式化日期
 * @param {Date} date - 日期对象
 * @returns {string} 格式化后的日期字符串 YYYY-MM-DD
 */
export function formatDate(date) {
  const year = date.getFullYear()
  const month = String(date.getMonth() + 1).padStart(2, '0')
  const day = String(date.getDate()).padStart(2, '0')
  return `${year}-${month}-${day}`
}
```

### 4.2 API 封装

```javascript
// api/sources.js
import axios from 'axios'

const API_BASE = '/api'

/**
 * 健康检查
 */
export async function healthCheck() {
  const response = await axios.get(`${API_BASE}/health`)
  return response.data
}

/**
 * 解析上传的文件
 * @param {File} file - JSON文件
 * @param {string} mode - 操作模式: 'dedup'(只查重复) 或 'full'(全部校验)
 */
export async function parseFile(file, mode = 'dedup') {
  const formData = new FormData()
  formData.append('file', file)
  formData.append('mode', mode)

  const response = await axios.post(`${API_BASE}/parse/file`, formData, {
    headers: {
      'Content-Type': 'multipart/form-data'
    }
  })
  return response.data
}

/**
 * 解析在线链接
 * @param {string} url - 书源JSON文件的URL
 * @param {string} mode - 操作模式: 'dedup' 或 'full'
 */
export async function parseUrl(url, mode = 'dedup') {
  const response = await axios.post(`${API_BASE}/parse/url`, { url, mode })
  return response.data
}
```

---

文档版本：v1.2
最后更新：2026-03-11